const XlColSlector = (state) => state.xlCol;

// products
const valueInputSearchSlector = (state) => state.valueInputSearch;
const ListProductSlector = (state) => state.ListProduct;
const ListSizeSlector = (state) => state.ListSize;
const ListSeacrhSlector = (state) => state.ListSeacrh;
const ListValueSlector = (state) => state.ListInput;
export {
  XlColSlector,
  valueInputSearchSlector,
  ListProductSlector,
  ListSizeSlector,
  ListSeacrhSlector,
  ListValueSlector,
};
